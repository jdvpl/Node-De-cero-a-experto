## pasos

```
npm i
  -b, --base      Es la base de la tabla de multiplicar      [number] [required]
  -l, --limite    Es el limite del multiplicador             [number] [required]
  -e, --ejecutar  Decide si listar o no listar por consola
                                                      [boolean] [default: false]
```